import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.damian.agendamientorural',
  appName: 'Agendamiento Médico Rural',
  webDir: 'dist'
};

export default config;
